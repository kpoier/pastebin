version: '3.8'

services:
  pastebin:
    build: 
      context: ..
      dockerfile: docker/Dockerfile
    container_name: pastebin
    ports:
      - "5000:5000"
    volumes:
      - ../uploads:/app/uploads
      - ../database.db:/app/database.db
    restart: always
